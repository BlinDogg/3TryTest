<UserControl x:Class="_3TryTest.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Height="{Binding UserControlHeight}" Width="480">
    <Grid Background="Black">
        <Grid.Clip>
            <EllipseGeometry RadiusY="240" Center="240, 240" RadiusX="240"/>
        </Grid.Clip>

        <Grid HorizontalAlignment="Center" Height="960" VerticalAlignment="Top" Width="480" Margin="0,-240,0,0" x:Name="grid">
            <Grid.RowDefinitions>
                <RowDefinition Height="50*" />
                <RowDefinition Height="50*" />
            </Grid.RowDefinitions>

            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#87CEEB" Offset="0"/>
                    <GradientStop Color="#87CEEB" Offset="0.5"/>
                    <GradientStop Color="#8B7355" Offset="0.5"/>
                    <GradientStop Color="#8B7355" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
        </Grid>
        <Grid>
            <Image Margin="0,125,0,496" Source="/Images/BBBB.png" Stretch="Fill"/>

        </Grid>
        
    </Grid>
</UserControl>
